<!DOCTYPE html>
<?
	setTimeout(wrapper(function()
	{
		echo('setTimeout wrapper');
	}),1000);

	var http = require('http');
	http.get("http://www.baidu.com", function(res) 
	{
  		echo("Response: " + res.statusCode+'<br>');
  		echo('http.get wrapper<br>');
	});