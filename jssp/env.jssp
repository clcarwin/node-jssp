<?
	for(var key in $_ENV) echo(key+':'+$_ENV[key]+'\n');

	var obj = $_ENV['external'];
    if(obj)
    {
    	echo('\n\nShow external object:\n');
    	for(var key in obj) echo(key+':'+obj[key]+'\n');
    }